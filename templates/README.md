# templates

Boilerplate build scripts, IP wrappers, and testbench harness skeletons, added here once they've
proven out on a real project (not authored speculatively ahead of need). Empty for now — this
directory exists so consuming projects have a stable path to `.framework/templates/...` once
content is added.
